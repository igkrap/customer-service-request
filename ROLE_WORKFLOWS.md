# Role Workflows

작성 기준: 현재 프로젝트의 프론트 메뉴 구성, `SecurityConfig`, 각 Controller/Service, 주요 화면 컴포넌트만 기준으로 정리했다. 기획상 추정 흐름은 제외했다.

## 공통 흐름

1. 사용자는 `/login` 화면에서 로그인하거나 같은 화면의 회원가입 폼으로 계정을 생성한다.
2. 회원가입 사용자는 `PENDING` 상태로 생성되며, 토큰 없이 승인 대기 안내를 받는다.
3. 관리자가 사용자 승인 화면에서 역할을 지정하고 승인해야 로그인 가능하다.
4. 로그인 성공 시 JWT와 사용자 정보가 localStorage에 저장되고, `PrivateRoute`가 인증된 사용자만 대시보드로 진입시킨다.
5. 로그인 후 모든 승인된 역할은 홈, 서비스 요청, 챗봇, 실시간 알림 WebSocket을 사용할 수 있다.
6. 역할이 없거나 승인되지 않은 사용자는 주요 화면 접근이 제한된다.

## ROLE_ADMIN: 관리자

### 주요 목적

전체 기준 정보와 운영 흐름을 관리하고, 사용자/프로젝트/서비스 요청/시스템 설정/실적 현황을 총괄한다.

### 업무 흐름

1. 홈에서 사용자, 회사, 프로젝트, 서비스 요청 요약을 확인한다.
2. 사용자 관리에서 신규 가입자를 조회하고, `ROLE_CUSTOMER`, `ROLE_MANAGER`, `ROLE_ADMIN` 중 역할을 지정해 승인하거나 거부한다.
3. 고객 사용자 승인 또는 역할 변경 시 고객 회사가 필수이며, 매니저와 관리자는 회사 연결이 제거된다.
4. 회사 관리에서 회사 정보를 등록, 수정, 삭제한다.
5. 프로젝트 관리에서 회사별 프로젝트를 등록, 수정, 삭제한다. 프로젝트 생성 시 라이선스 키가 자동 생성된다.
6. 사용자별 프로젝트 등록에서 고객 또는 매니저에게 프로젝트를 할당한다. 고객은 자기 회사 프로젝트만 할당 대상이 된다.
7. 서비스 요청 관리에서 전체 요청을 조회하고, 고객 대신 요청을 생성하거나 기존 요청을 수정/삭제할 수 있다.
8. 서비스 요청 생성/수정 시 관리자는 요청자, 담당자, 상태, 접수일자, 해결일자까지 관리할 수 있다.
9. 서비스 요청 상세에서 요청 기본 정보, 첨부파일, 처리 내용, 변경 기록, 타임라인을 확인한다.
10. 프로젝트 요청 승인 API 기준으로 고객의 프로젝트 등록 요청을 승인하거나 거부할 수 있다. 승인 시 실제 프로젝트가 생성된다.
11. 실적 현황 화면에서 월간 매니저별, 연간 매니저별, 회사별, 전체 처리 현황을 조회하고 필요한 화면에서는 Excel로 내보낸다.
12. 이메일 서버 설정, 이메일 템플릿, 공지 알림, LLM 설정, RAG 지식베이스를 관리한다.

### 접근 화면

| 메뉴 그룹 | 화면 |
| --- | --- |
| 메인 | 홈, 서비스 요청 관리 |
| 기준 정보 | 사용자 관리, 회사 관리, 프로젝트 관리, 사용자별 프로젝트 등록 |
| 실적 현황 | 월간 매니저별 실적 현황, 연간 매니저별 실적 현황, 회사별 실적 현황, 전체 실적 처리 현황 |
| 시스템 설정 | 이메일 서버 설정, 이메일 템플릿 관리, 공지 알림 전송, LLM 설정 관리, RAG 지식베이스 관리 |

## ROLE_MANAGER: 매니저

### 주요 목적

할당된 프로젝트와 접근 가능한 서비스 요청을 확인하고, 본인이 처리할 요청을 접수해 상태와 처리 결과를 갱신한다.

### 업무 흐름

1. 홈에서 할당된 프로젝트, 미배정 요청, 진행 중 요청, 보류 요청을 확인한다.
2. 프로젝트 조회에서 관리자가 할당한 프로젝트 목록을 조회 전용으로 확인한다.
3. 서비스 요청 처리 화면에서 본인에게 배정된 요청 또는 본인이 할당된 프로젝트의 요청을 조회한다.
4. 미배정 요청 또는 본인 담당 요청은 `IN_PROGRESS`로 변경할 수 있다.
5. `IN_PROGRESS`로 변경하면 해당 매니저가 담당자로 배정된다. 다른 매니저가 이미 처리 중인 요청은 가져올 수 없다.
6. 본인에게 배정된 요청만 `RESOLVED` 또는 `HOLD`로 변경할 수 있다.
7. 완료 처리 시 소요시간, 처리 내용, 해결 첨부파일을 입력한다.
8. 본인이 배정된 요청은 할당 해제할 수 있으며, 할당 해제 시 상태는 `OPEN`으로 돌아가고 처리 정보는 초기화된다.
9. 매니저는 서비스 요청의 전체 수정 화면이 아니라 상태 변경 전용 흐름을 사용한다.
10. 챗봇은 매니저의 담당 요청, 프로젝트, 현황 질문에 현재 데이터 컨텍스트를 붙여 응답한다.

### 접근 화면

| 메뉴 그룹 | 화면 |
| --- | --- |
| 메인 | 홈, 서비스 요청 처리, 프로젝트 조회 |
| 공통 | 챗봇, 실시간 알림 |

## ROLE_CUSTOMER: 고객/유저

### 주요 목적

본인 회사/프로젝트 기준으로 서비스 요청을 등록하고, 본인 요청의 처리 상태와 결과를 확인한다.

### 업무 흐름

1. 회원가입 후 관리자의 승인을 기다린다.
2. 관리자가 고객 역할과 회사를 지정해 승인하면 로그인할 수 있다.
3. 홈에서 할당된 프로젝트, 진행 중 요청, 보류 요청, 완료 요청을 확인한다.
4. 프로젝트 조회에서 관리자가 할당한 프로젝트 목록을 조회 전용으로 확인한다.
5. 서비스 요청 등록 화면에서 새 요청을 생성한다. 고객이 생성하는 요청은 항상 본인 고객 ID로 저장되고 초기 상태는 `OPEN`이다.
6. 서비스 요청 등록 시 제목, 설명, 우선순위, 프로젝트, 마감일, 첨부파일을 입력한다.
7. 고객이 선택할 수 있는 프로젝트는 본인 회사 프로젝트 중 관리자가 할당했고 계약 기간이 활성 상태인 프로젝트로 제한된다.
8. 본인이 등록한 서비스 요청 목록만 조회한다.
9. 본인 요청은 수정하거나 삭제할 수 있다. 단, 다른 고객의 요청은 조회/수정/삭제할 수 없다.
10. 매니저가 요청을 처리하면 상태 변경, 완료 처리, 알림/이메일 흐름을 통해 처리 결과를 확인한다.
11. 챗봇은 고객의 내 요청, 내 프로젝트, 현황 질문에 현재 데이터 컨텍스트를 붙여 응답한다.

### 접근 화면

| 메뉴 그룹 | 화면 |
| --- | --- |
| 메인 | 홈, 서비스 요청 등록, 프로젝트 조회 |
| 공통 | 챗봇, 실시간 알림 |

## 서비스 요청 상태 흐름

| 상태 | 의미 | 주요 변경 주체 |
| --- | --- | --- |
| `OPEN` | 신규 또는 미배정 요청 | 고객 생성, 관리자 생성, 매니저 할당 해제 |
| `IN_PROGRESS` | 매니저 처리 중 | 매니저, 관리자 |
| `HOLD` | 처리 보류 | 담당 매니저, 관리자 |
| `RESOLVED` | 처리 완료 | 담당 매니저, 관리자 |
| `CANCELLED` | 취소 | 관리자 수정 흐름에서 선택 가능 |

## 권한 요약

| 영역 | 관리자 | 매니저 | 고객 |
| --- | --- | --- | --- |
| 사용자 전체 조회/승인/거부/삭제 | 가능 | 불가 | 불가 |
| 본인 이메일/비밀번호/프로필 사진 변경 | 가능 | 가능 | 가능 |
| 회사 관리 | 가능 | 불가 | 불가 |
| 프로젝트 생성/수정/삭제 | 가능 | 불가 | 불가 |
| 프로젝트 조회 | 전체 조회 | 할당 프로젝트 조회 | 회사 및 할당 프로젝트 조회 |
| 사용자별 프로젝트 할당 | 가능 | API상 본인 할당만 가능, 메뉴 없음 | API상 본인 할당만 가능, 메뉴 없음 |
| 서비스 요청 조회 | 전체 | 본인 담당 또는 할당 프로젝트 요청 | 본인 요청 |
| 서비스 요청 생성 | 가능 | 불가 | 가능 |
| 서비스 요청 전체 수정/삭제 | 가능 | 불가 | 본인 요청만 가능 |
| 서비스 요청 상태 변경 | 가능 | 본인 담당 또는 미배정 요청 일부 가능 | 불가 |
| 프로젝트 등록 요청 생성 | 불가 | 불가 | 가능 |
| 프로젝트 등록 요청 승인/거부 | 가능 | 불가 | 불가 |
| 실적 리포트 | 가능 | 불가 | 불가 |
| 이메일/LLM/RAG/공지 설정 | 가능 | 불가 | 불가 |

## 현재 코드상 주의점

- `ProjectRequestList`와 `ProjectRequestApproval` 컴포넌트는 존재하지만 현재 `App.js` 메뉴에는 직접 연결되어 있지 않다. 백엔드 API 권한은 구현되어 있다.
- `AdminProjectMapping`에는 매니저 조회 전용 분기가 있지만 현재 메뉴 노출은 관리자 전용이다.
- `SecurityConfig`에서 일부 경로는 `authenticated`로 열고, 실제 세부 권한은 Controller의 `@PreAuthorize` 또는 메서드 내부 검사에서 제한한다.
- 서비스 요청 상세 조회, 상태/우선순위별 조회 같은 일부 조회 API는 경로 권한만 통과하면 추가 소유자 검사가 약한 편이다. 프론트 기본 진입은 역할별 목록 조회를 사용한다.
